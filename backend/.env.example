# Backend del sistema de triaje - variables de entorno
# Copia este archivo a .env y ajusta los valores.

# Modelo de triaje activo: 'roberta' o 'baseline'
TRIAGE_MODEL=roberta

# Rutas que normalmente no es necesario cambiar, pues en Docker se inyectan por compose
# MODELS_DIR=/app/modelos
# REPORTS_DIR=/app/reports
